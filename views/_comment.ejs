<li>
    <p> <%= comment.content %> </p>
    <small> <%= comment.user.name %> </small>

    <% if(locals.user && (locals.user._id == comment.user.id) )  { %>
    
        <a class="delete-comment-button" href="/comments/destroy/<%= comment._id %>"> delete comment </a>

    <% } %>
</li>